#include "ListaClientes.h"
/*
void ListaClientes::adicionaCliente(string nome, string cpf)
{
    clientes.push_back(Cliente(nome, cpf));
    
}

void ListaClientes::imprimeLista()
{
    for (int i = 0; i < clientes.size(); i++)
    {        
        clientes[i].imprime();
    }
}

string ListaClientes::getCpf(string cpf)
{
    for (int i = 0; i < clientes.size(); i++)
    {
        if (clientes[i].getCpf() == cpf)
            return clientes[i].getCpf();
    }
    return NULL;
}*/
